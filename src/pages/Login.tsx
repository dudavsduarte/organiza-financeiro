import { ArrowRight, BarChart3, CheckCircle2, Eye, EyeOff, LockKeyhole, Mail, UserRound } from 'lucide-react'
import { useState, type FormEvent } from 'react'
import { Navigate, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'
export function Login(){const{user,signIn,signUp,demoMode}=useAuth();const nav=useNavigate();const[register,setRegister]=useState(false);const[name,setName]=useState('');const[email,setEmail]=useState('cliente@demo.com');const[password,setPassword]=useState('123456');const[show,setShow]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState(false);if(user)return<Navigate to={user.access_status==='active'?'/dashboard':'/acesso'} replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const err=register?await signUp(name,email,password):await signIn(email,password);setBusy(false);if(err)setError(err);else nav('/dashboard')}
return <div className="auth-page"><section className="auth-hero"><div className="brand auth-brand"><div className="brand-mark"><BarChart3/></div><span>organiza.</span></div><div className="hero-copy"><span className="hero-kicker">FINANÇAS SEM COMPLICAÇÃO</span><h1>Organize hoje.<br/>Realize amanhã.</h1><p>Um painel completo para controlar gastos, definir metas e construir uma vida financeira mais tranquila.</p><ul><li><CheckCircle2/>Visão clara de receitas e despesas</li><li><CheckCircle2/>Orçamentos e metas personalizadas</li><li><CheckCircle2/>Relatórios simples e inteligentes</li></ul></div><div className="hero-orb orb-one"/><div className="hero-orb orb-two"/></section><section className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><div><span className="eyebrow">ACESSO EXCLUSIVO</span><h2>{register?'Crie sua conta':'Bem-vinda de volta'}</h2><p>{register?'Use o mesmo e-mail informado na compra.':'Entre para acessar seu painel financeiro.'}</p></div>{register&&<label>Nome completo<div className="input-icon"><UserRound/><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Seu nome"/></div></label>}<label>E-mail<div className="input-icon"><Mail/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@email.com"/></div></label><label>Senha<div className="input-icon"><LockKeyhole/><input required minLength={6} type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mínimo de 6 caracteres"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="alert error">{error}</div>}<button className="btn primary auth-submit" disabled={busy}>{busy?'Aguarde...':register?'Criar conta':'Entrar'}<ArrowRight size={18}/></button>{demoMode&&<div className="demo-note">Modo demonstração: qualquer e-mail e senha com 6 caracteres liberam o painel.</div>}<p className="auth-switch">{register?'Já possui conta?':'Primeiro acesso após a compra?'} <button type="button" onClick={()=>setRegister(!register)}>{register?'Entrar':'Criar conta'}</button></p></form></section></div>}
